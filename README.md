# eVTOL Tools

Toolbox for eVTOL vehicle configuration, sizing, and mission analysis tools.

## Setup

For editable local install, use

```bash
pip install -e .
```

or

```bash
pip install -e ".[dev]"
```
to install optional dependencies for testing and linting.